#include <stdio.h>

int main() {
    // what will each print? answers in comments
    printf("We have %d coins in the bank\n", 100); // We have 100 coins in the bank
    printf("We have %f coins in the bank\n", 125.7); // We have 125.700000 coins in the bank
    printf("Year = %d\n", 2020); // Year = 2020
    printf("My avg = %f", 95.13); // My avg = 95.130000a
    printf("a = %d, b = %d, sum = a + b = %d + %d = %d\n", 3, 5, 3, 5, 3 + 5);
    // a = 3, b = 5, sum = a + b = 3 + 5 = 8
    return 0;
}